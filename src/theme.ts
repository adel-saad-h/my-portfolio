import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: { main: "#fca311" },
        secondary: { main: "#e5e5e5" },
        success: { main: "#ffffff" },
        background: { default: "#344e41" },
    }
})